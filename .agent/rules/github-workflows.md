---
trigger: always_on
---

1. Create one workflow that build on pull request
2. Create one workflow that builds and publish on merge to main and develop
2.1 The publish to develop must have its own develop tag
2.1 The publish to main must have its own prod tag